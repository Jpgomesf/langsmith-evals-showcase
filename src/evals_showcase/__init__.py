"""evals_showcase — a multi-scenario eval gym for LangSmith.

Each ``scenarios/<name>`` package holds a small app-under-test plus the
experiment that evaluates it. Reusable evaluators live in ``evaluators/``.
"""

__all__ = ["config", "models"]
